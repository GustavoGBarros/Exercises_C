// Declare uma variável do tipo inteiro chamada numero e uma outra variável do mesmo tipo chamada x atribuindo as duas concomitante o valor 100.
#include <stdio.h>

int main() {
    int numero, x;
    numero = x = 100;
    printf("numero = %i\n", numero);
    printf("x = %i\n", x);
    return 0;
}