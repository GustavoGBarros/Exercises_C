// Declare de uma variável do tipo caracter chamada letra e declare outra variável do mesmo tipo chamada letra_a atribuindo-lhe o valor 'a'.
#include <stdio.h>

int main() {
    char letra;
    char letra_a = 'a'; 
    printf("O valor da variável letra_a é: %c", letra_a);
    return 0;
}